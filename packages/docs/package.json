{
  "name": "@lest/docs",
  "private": true,
  "description": "API documentation in JSON form.",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "format": "prettier --config ../../.prettierrc --write ."
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
