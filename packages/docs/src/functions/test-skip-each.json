{
  "name": "test.skip.each",
  "aliases": ["it.skip.each", "xtest.each", "xit.each"],
  "description": [
    "Dummy version of `test.each()` which doesn't run the given test cases.",
    "They will appear in test results as being skipped (this is not implemented yet, see LEST-88)."
  ],
  "parameters": [
    {
      "name": "testCases",
      "type": "array",
      "items": "any"
    }
  ],
  "returns": [
    {
      "type": "function",
      "parameters": [
        {
          "name": "name",
          "type": "string"
        },
        {
          "name": "fn",
          "type": "function",
          "parameters": [
            {
              "name": "...",
              "type": "any"
            }
          ]
        },
        {
          "name": "timeout",
          "type": "number",
          "optional": true
        }
      ]
    }
  ]
}
