test.todo("checks all lua executable names in order");

test.todo("returns the first found path");

test.todo("throws an error if no path could be found");

describe("with luaCommand", () => {
	test.todo("returns the resolved path if found");

	test.todo("throws an error if the given command could not be resolved");
});
