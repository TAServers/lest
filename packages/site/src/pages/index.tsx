import React from "react";
import Layout from "@theme/Layout";

const Home = (): JSX.Element => (
	<Layout title="Home" description="Zero dependency Lua testing framework in the style of Jest">
		<main>
			<p>This page is a work in progress</p>
		</main>
	</Layout>
);
export default Home;
