{
  "name": "@taservers/lest",
  "description": "Lua testing framework in the style of Jest.",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "prepare": "husky install",
    "format:site": "prettier --check packages/site",
    "build:lua": "npm run build --workspace=packages/lua",
    "build:site": "npm run build --workspace=packages/site",
    "build:luals-annotations": "npm run build --workspace=packages/luals-annotations",
    "test:luals-annotations": "npm run test --workspace=packages/luals-annotations",
    "test:lua": "npm run test --workspace=packages/lua",
    "annotate:luals": "npm run start --workspace=packages/luals-annotations"
  },
  "workspaces": [
    "packages/lua",
    "packages/docs",
    "packages/site",
    "packages/luals-annotations"
  ],
  "devDependencies": {
    "@commitlint/cli": "^17.5.1",
    "@commitlint/config-conventional": "^17.4.4",
    "husky": "^8.0.3",
    "prettier": "^2.8.7",
    "rimraf": "^5.0.0"
  }
}
